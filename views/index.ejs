<!DOCTYPE html>
<html lang="id" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio | Bintang Anggaraksa</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        dark: '#0f172a', // Slate 900
                        darker: '#020617', // Slate 950
                        accent: '#34d399', // Emerald 400
                        secondary: '#94a3b8' // Slate 400
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-dark text-slate-200 antialiased selection:bg-accent selection:text-darker">

    <nav class="fixed w-full z-50 bg-dark/90 backdrop-blur-sm border-b border-slate-800">
        <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
            <a href="#" class="text-xl font-bold tracking-tighter text-white">Bintang<span class="text-accent">.dev</span></a>
            <div class="hidden md:flex space-x-8 text-sm font-medium text-secondary">
                <a href="#about" class="hover:text-accent transition">About</a>
                <a href="#skills" class="hover:text-accent transition">Skills</a>
                <a href="#projects" class="hover:text-accent transition">Projects</a>
                <a href="#contact" class="hover:text-accent transition">Contact</a>
            </div>
        </div>
    </nav>

    <section class="min-h-screen flex items-center justify-center pt-20">
        <div class="max-w-4xl mx-auto px-6 text-center">
            <p class="text-accent font-medium mb-4 animate-pulse">Hi, I'm Bintang Anggaraksa Lincah Nugraha</p>
            <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight tracking-tight">
                <%= data.hero.role %>
            </h1>
            <p class="text-lg md:text-xl text-secondary mb-10 max-w-2xl mx-auto">
                "<%= data.hero.tagline %>"
            </p>
            <div class="flex justify-center gap-4">
                <a href="#projects" class="px-8 py-3 bg-accent text-darker font-bold rounded-full hover:bg-emerald-300 transition hover:scale-105">Lihat Proyek</a>
                <a href="#contact" class="px-8 py-3 border border-slate-700 rounded-full hover:border-accent hover:text-accent transition">Hubungi Saya</a>
            </div>
        </div>
    </section>

    <section id="about" class="py-24 bg-darker">
        <div class="max-w-6xl mx-auto px-6">
            <h2 class="text-3xl font-bold text-white mb-12 flex items-center">
                <span class="text-accent mr-2">01.</span> About Me
            </h2>
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div class="space-y-6 text-secondary leading-relaxed">
                    <p><%= data.about.desc %></p>
                    <p>Selain coding, saya juga aktif menyeimbangkan hidup dengan hobi seperti:
                        <span class="text-accent"><%= data.about.hobbies.join(", ") %></span>.
                    </p>
                    
                    <div class="pt-4">
                        <h3 class="text-white font-semibold mb-3">Minat Utama:</h3>
                        <ul class="flex flex-wrap gap-3">
                            <% data.about.interests.forEach(interest => { %>
                                <li class="px-4 py-1 bg-slate-800 rounded-full text-xs text-accent border border-slate-700">
                                    <%= interest %>
                                </li>
                            <% }) %>
                        </ul>
                    </div>
                </div>
                <div class="relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-accent to-blue-600 rounded-lg blur opacity-25 group-hover:opacity-50 transition duration-1000 group-hover:duration-200"></div>
                    <div class="relative bg-slate-800 rounded-lg aspect-video flex items-center justify-center border border-slate-700">
                        <span class="text-slate-500">[Foto Profil / Setup Robotik]</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="skills" class="py-24 bg-dark">
        <div class="max-w-6xl mx-auto px-6">
            <h2 class="text-3xl font-bold text-white mb-12 flex items-center">
                <span class="text-accent mr-2">02.</span> Tech Stack & Skills
            </h2>
            
            <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-12">
                <% data.skills.forEach(skill => { %>
                    <div class="p-4 bg-slate-800/50 rounded-lg border border-slate-700 hover:border-accent transition hover:-translate-y-1 group">
                        <h4 class="text-accent text-sm mb-1"><%= skill.category %></h4>
                        <p class="text-white font-semibold group-hover:text-white"><%= skill.name %></p>
                    </div>
                <% }) %>
            </div>

            <div class="bg-slate-800/30 p-8 rounded-2xl border border-slate-800">
                <h3 class="text-xl font-bold text-white mb-6 text-center">Soft Skills</h3>
                <div class="flex flex-wrap justify-center gap-6">
                    <% data.softSkills.forEach(skill => { %>
                        <div class="flex items-center space-x-2 text-secondary">
                            <svg class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                            <span><%= skill %></span>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </section>

    <section id="projects" class="py-24 bg-darker">
        <div class="max-w-6xl mx-auto px-6">
            <h2 class="text-3xl font-bold text-white mb-12 flex items-center">
                <span class="text-accent mr-2">03.</span> Featured Projects
            </h2>
            <div class="grid md:grid-cols-2 gap-8">
                <% data.projects.forEach(project => { %>
                    <div class="group bg-slate-900 rounded-xl overflow-hidden border border-slate-800 hover:border-slate-600 transition">
                        <div class="p-8">
                            <div class="flex justify-between items-start mb-4">
                                <h3 class="text-2xl font-bold text-white group-hover:text-accent transition"><%= project.title %></h3>
                                <a href="<%= project.link %>" target="_blank" class="text-secondary hover:text-white">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
                                </a>
                            </div>
                            <p class="text-secondary mb-6 h-20"><%= project.desc %></p>
                            <div class="flex flex-wrap gap-2 mt-auto">
                                <% project.tech.forEach(tech => { %>
                                    <span class="text-xs font-mono text-accent bg-accent/10 px-3 py-1 rounded-full"><%= tech %></span>
                                <% }) %>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </section>

    <section id="contact" class="py-24 bg-dark text-center">
        <div class="max-w-2xl mx-auto px-6">
            <h2 class="text-4xl font-bold text-white mb-6">Let's Connect</h2>
            <p class="text-secondary mb-10">
                Saya selalu terbuka untuk diskusi mengenai AI, Web Development, atau kolaborasi proyek robotik.
            </p>
            <a href="mailto:<%= data.contact.email %>" class="inline-block px-8 py-4 bg-transparent border-2 border-accent text-accent font-bold rounded-lg hover:bg-accent/10 transition">
                Say Hello
            </a>

            <div class="mt-16 flex justify-center space-x-8">
                <a href="https://github.com/<%= data.contact.github %>" class="text-secondary hover:text-white hover:-translate-y-1 transition">GitHub</a>
                <a href="<%= data.contact.linkedin %>" class="text-secondary hover:text-white hover:-translate-y-1 transition">LinkedIn</a>
                <a href="https://instagram.com/<%= data.contact.instagram %>" class="text-secondary hover:text-white hover:-translate-y-1 transition">Instagram</a>
            </div>
            
            <footer class="mt-20 text-slate-600 text-sm">
                <p>Designed & Built by <span class="text-slate-500">Bintang Anggaraksa</span></p>
                <p class="mt-2">Powered by Node.js & Tailwind CSS</p>
            </footer>
        </div>
    </section>

</body>
</html>